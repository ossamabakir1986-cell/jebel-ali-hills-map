Hayat Luxury GIS v3.3.36 - Plot Color Filter + Unified Filter Tab Styling

Built on v3.3.35 Auto Date + Multi-select Filters.

Changes:
- The old visible "All GFA" filter position is replaced with the plot color multi-select filter.
- The plot color filter label now says "All plot colors" instead of "All statuses".
- Plot color options are refreshed from actual plot color and second color data.
- The GFA filter is hidden from the main filter row so it does not confuse users. GFA data and popup/details fields remain preserved.
- Main filter dropdown tabs now use the same dark emerald fill and gold text styling as the All agents tab.
- Styling applies consistently to Agent, Type, Feature, Phase, Plot Colors, and Pricing filter tabs on Admin and Agent maps.

Preserved from v3.3.35:
- Auto Last Date Updated on add/edit.
- Multi-select agents, phases, features, and colors.
- Popup/details refresh.
- Manual GFA allowed override.
- Dedicated Last Date Updated field and display label.
- 493 inventory records and 2,963 PA labels.
- Size/GFA calculations, agent/mobile autofill, PA + Add, Move Plot, fast filters, Agent view-only.
