Hayat Luxury GIS v3.3.41 - Stable Filter Tabs + Working All Buttons

Built on v3.3.40 Stable Filter Tabs.

Fix:
- The All button inside every filter dropdown now visibly selects/ticks all choices.
- The Clear button unticks all choices.
- Both actions refresh the map immediately.
- Applies to Agents, Statuses, Types, Features, Phases, GFA, and Pricing filters.

Behavior note:
- Selecting all choices gives the same map result as no restriction, but now it is visible because all checkboxes become checked.
- Clear returns the filter to no selected values.

Preserved:
- v3.3.34 data update, v3.3.35 auto Last Date Updated and multi-filter logic, v3.3.40 stable filter dropdown layout.
